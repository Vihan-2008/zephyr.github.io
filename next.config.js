/** @type {import('next').NextConfig} */
const nextConfig = {
  //reactStrictMode: true, // Enables strict mode for React
  //output: 'export',      // Configures the app for static export
};

module.exports = nextConfig;
